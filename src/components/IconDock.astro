---
import BrandIcon from "./BrandIcon.astro";

const { links, note = "Find me elsewhere" } = Astro.props;
---

<footer class="site-footer">
  <p class="site-footer-note">{note}</p>
  <div class="icon-dock" aria-label="External profiles">
    {links.map((link) => (
      <a
        class="dock-link"
        href={link.href}
        target="_blank"
        rel="noreferrer"
        aria-label={link.label}
        title={link.label}
      >
        <BrandIcon icon={link.icon} class="dock-icon" />
      </a>
    ))}
  </div>
</footer>
